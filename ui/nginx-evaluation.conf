server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }

    location /api {
        proxy_set_header Host $http_host;
        proxy_pass http://peoplemover-api:8080/api;
    }
}
