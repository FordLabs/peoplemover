project(':ui') {}
project(':api') {}

task build(dependsOn: [':api:build', ':ui:npm_run_buildfeature'])

task hotDeployBranch(type: Exec, dependsOn: 'build') {
    commandLine "./gradlew", ":api:pushBranchAPI", ":ui:pushBranchUI"
}
